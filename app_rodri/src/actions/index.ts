import { auth } from './auth';
import { hp, resources, channel, mythic, domain, feat, hero, combat, rest, log } from './state';
import { allies, buffs, settings, notes, gold, backup } from './misc';
import { spells, dice } from './spells';

export const server = {
  auth,
  hp,
  resources,
  channel,
  mythic,
  domain,
  feat,
  hero,
  combat,
  rest,
  log,
  allies,
  buffs,
  settings,
  notes,
  gold,
  backup,
  spells,
  dice,
};
